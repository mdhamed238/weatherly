version: '3.8'

services:
  weatherly-api:
    build: .
    container_name: weatherly-api
    ports:
      - "8080:8080"
    environment:
      - WEATHER_API_KEY=${WEATHER_API_KEY:-dummy_api_key_for_development}
      - SERVER_PORT=8080
    restart: unless-stopped
